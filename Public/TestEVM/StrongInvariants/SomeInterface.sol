pragma solidity 0.8.24;

interface SomeInterface {
    function repay() external returns (uint256);
    function havocECF() external returns (uint256);
    function havocALL() external returns (uint256);
}
